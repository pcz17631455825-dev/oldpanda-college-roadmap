"use client"; import {useState} from "react"; import type{Species}from "@/data/species";
export function SpeciesCard({species}:{species:Species}){const[open,setOpen]=useState(false);return <section className="species"><div className="species-label">🎭 娱乐结果，仅供开心</div><button className="species-head" onClick={()=>setOpen(!open)} aria-expanded={open}><span className="species-emoji">{species.emoji}</span><span><b>你是「{species.name}」</b><small>{species.catchphrase}</small></span><span className="chevron">{open?"−":"+"}</span></button>{open&&<div className="species-body"><p>{species.description}</p><div><span>典型行为</span><strong>{species.behavior}</strong></div><div><span>保护等级</span><strong>{species.conservationStatus}</strong></div><button className="share" type="button">分享物种卡（即将上线）</button></div>}</section>}
