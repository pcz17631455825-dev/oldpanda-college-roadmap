"use client";import Link from"next/link";import{useEffect,useState}from"react";import{useRouter}from"next/navigation";import{computeResult,hollandLabels,valueLabels}from"@/lib/assessment";import{matchSpecies}from"@/lib/speciesMatcher";import{SpeciesCard}from"@/components/SpeciesCard";import type{AssessmentResult}from"@/types";
const icon={"升学派":"◈","就业派":"↗","体制派":"▦"};
export default function ResultsPage(){const router=useRouter(),[result,setResult]=useState<AssessmentResult|null>(null),[loading,setLoading]=useState(true);useEffect(()=>{const saved=localStorage.getItem("career-assessment-v2-result");if(!saved){router.replace("/assessment");return}const{answers,duration}=JSON.parse(saved)as{answers:number[][];duration:number};const timer=setTimeout(()=>{setResult(computeResult(answers,duration));setLoading(false)},1100);return()=>clearTimeout(timer)},[router]);if(loading||!result)return <main className="loading"><div className="loader">✦</div><p>正在拼合你的方向画像…</p><small>兴趣、价值观和选择偏好正在对齐</small></main>;const creature=matchSpecies(result);return <main className="results"><div className="result-top"><Link href="/" className="brand"><span>✦</span> 方向实验室</Link><p>你的测评结果</p></div><section className={`faction-card ${result.faction==="升学派"?"study":result.faction==="就业派"?"work":"stable"}`}><div className="faction-icon">{icon[result.faction]}</div><p>你的发展倾向</p><h1>{result.faction}</h1><span>{result.faction==="升学派"?"越学越有底气":result.faction==="就业派"?"在实践中把能力变现":"在稳定中建立长期优势"}</span></section><section className="result-section"><p className="eyebrow">HOLLAND INTEREST CODE</p><h2>{result.hollandCode}</h2><p className="code-explain">{result.hollandCode.split("").map(key=>hollandLabels[key as keyof typeof hollandLabels]).join(" · ")}。结果来自行为情境、兴趣活动和价值排序的综合画像，不是人格标签。</p><div className="bars">{Object.entries(result.hollandScores).map(([key,score])=><div key={key}><span>{key}</span><i><b style={{width:`${score}%`}}/></i><em>{score}</em></div>)}</div></section><section className="result-section path"><p className="eyebrow">YOUR NEXT CHAPTER</p><h2>适合你的发展节奏</h2><p>{result.factionDescription}</p><div className="values">{result.valuesRanking.slice(0,3).map((key,index)=><span key={key}><b>TOP {index+1}</b>{valueLabels[key]}</span>)}</div></section><SpeciesCard species={creature}/><Link className="primary wide" href="/majors">查看为你推荐的专业 <span>→</span></Link><p className="disclaimer">本测评用于职业探索与自我反思，不是临床心理诊断；结果仅供参考，不构成志愿填报或职业选择的唯一依据。</p></main>}
